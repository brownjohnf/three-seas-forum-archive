= will_paginate

%ol
  - @users.each do |user|
    %li
      = link_to user.username, user
      |
      = user.posts.count
      posts
      |
      = user.rank.title unless user.rank.nil?

= will_paginate
